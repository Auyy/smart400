@model Smart400.Models.As400Response
@{
    ViewData["Title"] = "Home Page";
}

<table class="table">
    <thead>
        <tr>
            <th>
                Smart400
            </th>
            
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.TextSmart)
        {
            <tr>
                <td>
                    @item
                </td>
            </tr>
        }

    </tbody>
</table>

